function countLetters(message) {
    // var length = 0;
    // for (var i = 0; i < message.length; i++) length += 1;
    // return length;
    document.writeln(message);
    return message.length;
}

// document.write(countLetters('hello how are you?'));